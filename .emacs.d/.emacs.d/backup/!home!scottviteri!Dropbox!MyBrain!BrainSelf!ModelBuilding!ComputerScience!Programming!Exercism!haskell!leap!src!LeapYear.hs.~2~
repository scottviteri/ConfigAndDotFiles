module LeapYear (isLeapYear) where

isLeapYear :: Integer -> Bool
isLeapYear year = year > 4


-- isLeapYear
{-
isLeapYear year =
    if (mod year 4 == 0)
     then if (mod year 100 == 0)
      then if (mod year 400 == 0)
       then True
       else False
      else True
     else False
-}
λ> putStrLn("hello")
