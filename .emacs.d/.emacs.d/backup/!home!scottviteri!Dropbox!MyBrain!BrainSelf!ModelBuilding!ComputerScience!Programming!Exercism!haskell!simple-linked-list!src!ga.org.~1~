
#+BEGIN_SRC haskell

fdsa :: Num a => a -> a -> a
fdsa x y = x^2 + y^2

#+END_SRC

#+RESULTS:
#+begin_example
<interactive>:3:12-14: warning: [-Wtype-defaults]
    • Defaulting the following constraints to type ‘Integer’
        (Integral b0) arising from a use of ‘^’ at <interactive>:3:12-14
        (Num b0) arising from the literal ‘2’ at <interactive>:3:14
    • In the first argument of ‘(+)’, namely ‘x ^ 2’
      In the expression: x ^ 2 + y ^ 2
      In an equation for ‘fdsa’: fdsa x y = x ^ 2 + y ^ 2

<interactive>:3:18-20: warning: [-Wtype-defaults]
    • Defaulting the following constraints to type ‘Integer’
        (Integral b0) arising from a use of ‘^’ at <interactive>:3:18-20
        (Num b0) arising from the literal ‘2’ at <interactive>:3:20
    • In the second argument of ‘(+)’, namely ‘y ^ 2’
      In the expression: x ^ 2 + y ^ 2
      In an equation for ‘fdsa’: fdsa x y = x ^ 2 + y ^ 2
#+end_example

#+BEGIN_SRC haskell
fdsa 3 4
#+END+SRC
